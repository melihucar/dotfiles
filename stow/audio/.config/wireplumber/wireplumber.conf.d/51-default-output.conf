# Intentionally empty. The default output is chosen by monitor NAME at login by
# ~/.local/bin/audio-to-monitor (run from hyprland.conf), because the HDMI port order
# changes between kernels. A fixed "hdmi-stereo-extra1" rule here picked the wrong screen.
